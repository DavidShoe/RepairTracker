@model IList<RepairTracker.DBModels.RepairNote>

@if (Model is not null)
{
    for (int i = 0; i < Model.Count; i++)
    {
        @await Html.PartialAsync("_NoteLinePartialView", Model[i], new ViewDataDictionary(ViewData) { { "Index", i } })
    }
}
